# django_automation_tester


### How to install custom pip package:

- Open a terminal and locate were the setup.py directory

```bash
pip install -e .
```

- Then you can run the script like: `creata <arguments>`